<div class="chatWindow">
    <header class="chatWindow__header">
        <img class="chatWindow__header-avatar" src={{chat.avatar}}></image>
        <span class="chatWindow__header-name">{{chat.name}}</span>
        <div class="chatWindow__header-ellipsis">
            {{{ ContextMenu className=userSettings.className icon=userSettings.icon items=userSettings.items }}}
        </div>
    </header>
    <div class="chatWindow__content">
        {{{ MessageList messageList=messageList }}}
    </div>
    <footer class="chatWindow__footer">
        {{{ MessageInput }}}
    </footer>

    {{#with modalConfigs.addUser}}
        {{#if showModal}}
            {{{ Modal title="Добавить пользователя"
                content=content
                buttonLabel="Добавить"
                onClick=onClose
                onSubmit=onSubmit
                ref="addUser"
            }}}
        {{/if}}
    {{/with}}

    {{#with modalConfigs.deleteUser}}
        {{#if showModal}}
            {{{ Modal title="Удалить пользователя"
                content=content
                buttonLabel="Удалить"
                onClick=onClose
                onSubmit=onSubmit
                ref="deleteUser"
            }}}
        {{/if}}
    {{/with}}
</div>
